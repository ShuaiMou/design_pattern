懒汉式和饿汉式的构造函数都是私有的，彻底断开类使用构造函数来得到类的实例的通道，
但是这样也使得类失去类的多态性。

懒汉式中，对静态工厂方法进行了同步处理（为了防止多线程环境中产生多个实例），而饿汉式
中则不存在这样的问题。

懒汉式中将类对自己的实例化延迟到第一次被引用的时候。而在饿汉式中是在类被加载的时候实例化，
这样多次加载会造成多次实例化。但是懒汉式中由于使用类同步处理，在反应上要比饿汉式慢一些。